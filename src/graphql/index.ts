/*
 * graphql/index.ts
 * GraphQL Index
 */

/*
 * Module Dependencies
 */

import s from './schema';
import r from './resolvers';
/*
 * Resolvers
 */

export const schema = s;
export const resolvers = r;
